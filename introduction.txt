#include <stdio.h> 
int main() { 
	printf(“Hello World”); //ต้องปิดด้วย ; เสมอ
}
scope ดูจากปีกกา ภาษาซีเป็นภาษาที่ต้องบอกtypeให้กับทุกอย่าง
ต้อง save ทุกครั้ง จากกากบาทจะเปลี่ยนเป็นจุด
วิธี compile gcc ชื่อไฟล์ -o ชื่อoutput
วิธีการรัน ./ชื่อไฟล์
